# Angular, ng-packagr and the Analog Vite plugin expect a real node_modules
# layout, so use the node-modules linker rather than Yarn's default PnP.
nodeLinker: node-modules

# Keep the package cache out of the repo (no zero-installs).
enableGlobalCache: true

# Deterministic, CI-friendly output.
enableTelemetry: false
