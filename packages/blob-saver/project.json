{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "blob-saver",
  "projectType": "library",
  "sourceRoot": "packages/blob-saver/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/blob-saver"],
      "options": {
        "command": "ng-packagr -p packages/blob-saver/ng-package.json -c packages/blob-saver/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/blob-saver"],
      "options": {
        "command": "vitest run --config packages/blob-saver/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/blob-saver/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/blob-saver/tsconfig.lib.json --noEmit",
          "tsc -p packages/blob-saver/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
