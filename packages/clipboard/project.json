{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "clipboard",
  "projectType": "library",
  "sourceRoot": "packages/clipboard/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/clipboard"],
      "options": {
        "command": "ng-packagr -p packages/clipboard/ng-package.json -c packages/clipboard/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/clipboard"],
      "options": {
        "command": "vitest run --config packages/clipboard/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/clipboard/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/clipboard/tsconfig.lib.json --noEmit",
          "tsc -p packages/clipboard/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
