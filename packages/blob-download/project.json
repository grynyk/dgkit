{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "blob-download",
  "projectType": "library",
  "sourceRoot": "packages/blob-download/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/blob-download"],
      "options": {
        "command": "ng-packagr -p packages/blob-download/ng-package.json -c packages/blob-download/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/blob-download"],
      "options": {
        "command": "vitest run --config packages/blob-download/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/blob-download/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/blob-download/tsconfig.lib.json --noEmit",
          "tsc -p packages/blob-download/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
