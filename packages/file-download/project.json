{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "file-download",
  "projectType": "library",
  "sourceRoot": "packages/file-download/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/file-download"],
      "options": {
        "command": "ng-packagr -p packages/file-download/ng-package.json -c packages/file-download/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/file-download"],
      "options": {
        "command": "vitest run --config packages/file-download/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/file-download/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/file-download/tsconfig.lib.json --noEmit",
          "tsc -p packages/file-download/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
