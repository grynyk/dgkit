{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "intersection-observer",
  "projectType": "library",
  "sourceRoot": "packages/intersection-observer/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/intersection-observer"],
      "options": {
        "command": "ng-packagr -p packages/intersection-observer/ng-package.json -c packages/intersection-observer/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/intersection-observer"],
      "options": {
        "command": "vitest run --config packages/intersection-observer/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/intersection-observer/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/intersection-observer/tsconfig.lib.json --noEmit",
          "tsc -p packages/intersection-observer/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
