{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "format",
  "projectType": "library",
  "sourceRoot": "packages/format/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/format"],
      "options": {
        "command": "ng-packagr -p packages/format/ng-package.json -c packages/format/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/format"],
      "options": {
        "command": "vitest run --config packages/format/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/format/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/format/tsconfig.lib.json --noEmit",
          "tsc -p packages/format/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
