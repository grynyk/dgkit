{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mutation-observer",
  "projectType": "library",
  "sourceRoot": "packages/mutation-observer/src",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/mutation-observer"],
      "options": {
        "command": "ng-packagr -p packages/mutation-observer/ng-package.json -c packages/mutation-observer/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/packages/mutation-observer"],
      "options": {
        "command": "vitest run --config packages/mutation-observer/vitest.config.ts --coverage"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/mutation-observer/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "tsc -p packages/mutation-observer/tsconfig.lib.json --noEmit",
          "tsc -p packages/mutation-observer/tsconfig.spec.json --noEmit"
        ]
      }
    }
  }
}
