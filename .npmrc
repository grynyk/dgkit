# pnpm workspace settings (safe to commit — contains no secrets)
# Hoist Angular/build tooling so ng-packagr, jest-preset-angular and the
# Analog Vite plugin can resolve compiler packages from the workspace root.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
engine-strict=false
provenance=false
